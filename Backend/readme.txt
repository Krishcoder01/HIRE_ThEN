   Implement user registration and login with JWT-based authentication.
   Secure password storage (e.g., using bcrypt) and proper input validation.

 Transaction Management:
 Create endpoints for CRUD operations on transactions.
 Each transaction must include: description, amount, type (expense/income), category, and date.

●     Budget Management:

○     Allow users to set budgets per category.

○     Provide an endpoint to fetch budget alerts (e.g., when spending nears the budget).

    Database:
     Use MongoDB (Atlas or local) with well-designed schemas and indexing.
